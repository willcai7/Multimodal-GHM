# Multimodal Learning with GHM

This repository contains the official implementation of the paper:

**A Statistical Theory of Contrastive Pre-training and Multimodal Generative AI**  
Oko Kazusato, Licong Lin, Yuhang Cai, Song Mei  
Paper: https://arxiv.org/abs/2501.04641 

## Setup 

To get started, create a new `uv` environment (Python 3.10) and install the package in editable mode:

```shell
uv venv --python 3.10
source .venv/bin/activate
uv pip install -e .
```

For the exact tested environment recorded in `uv.lock`, run:

```shell
uv sync
```

### Tested Environment

The dependency ranges in `pyproject.toml` are compatible lower bounds. The current tested `uv` environment uses:

- Python 3.10.20
- `numpy==2.2.6`
- `torch==2.9.0`
- `tqdm==4.67.1`
- `transformers==4.57.1`
- `wandb==0.22.3`
- `s3fs==2025.9.0`
- `matplotlib==3.10.9`
- `ipykernel==7.2.0`
- `nbconvert==7.17.1`
- `pytest==9.0.3`
- `huggingface-hub==0.36.0`

## Reproduction Workflow

The top-level reproduction scripts are intended to make the execution order explicit. Run them from the repository root after activating the environment.

### Option A: Reproduce from Released Artifacts

This is the recommended route for regenerating evaluation JSON files and figures without retraining.

```bash
python scripts/download_ckpt.py
python scripts/download_data.py
bash scripts/reproduce_eval.sh
bash scripts/reproduce_figures.sh
```

- `scripts/download_ckpt.py` downloads released training artifacts from `https://huggingface.co/faro1219/multimodal-ghm/tree/main/logs` and stages them as `checkpoints/{CLIP,CDM,VLM}`.
- `scripts/download_data.py` downloads the ImageNet zero-shot result tensor from the gated BiasCLIP dataset at `https://huggingface.co/datasets/BiasCLIP/BiasCLIP/tree/main`. The file is stored as `figures/data/imagenet-data/similarities_targets.pt`. You may need to accept the dataset terms and authenticate with `huggingface-cli login` or `HF_TOKEN`.
- `scripts/reproduce_eval.sh` consumes `checkpoints/{CLIP,CDM,VLM}` and regenerates the small GHM JSON files under `figures/data/ghm-data/`.
- `scripts/reproduce_figures.sh` executes the plotting notebooks and writes PDFs under `figures/output/`.

### Option B: Full Retraining

Full retraining is substantially more expensive. The wrapper below runs the experiment scripts in the order needed by the sequential CDM/VLM models, then moves completed `logs/{CLIP,CDM,VLM}` artifacts into `checkpoints/{CLIP,CDM,VLM}` for evaluation.

```bash
bash scripts/reproduce_train.sh
bash scripts/reproduce_eval.sh
bash scripts/reproduce_figures.sh
```

Use `DRY_RUN=1 bash scripts/<script>.sh` with the reproduction wrapper scripts to print the planned actions without launching training, evaluation, or notebook rendering.

The Hugging Face download scripts also provide lightweight checks:

```bash
python scripts/download_ckpt.py --dry-run
python scripts/download_data.py --check-only
```

`scripts/download_data.py --check-only` verifies access to the Fig. 7 ImageNet tensor without downloading the file, which is about 14.9 GiB.

### Display-Item Map

Figures marked as illustration examples are manually authored schematic figures, not experimental results, and are not generated by the training/evaluation pipeline.

| Display item | Evaluation data source | Plot/render source | Expected output |
| --- | --- | --- | --- |
| Fig. 1a-b JGHM model and example | None; illustration example, not an experimental result | Manual illustration artwork, e.g. Adobe Illustrator/Keynote | `figures/output/Fig1-a-JGHM-model.pdf`, `figures/output/Fig1-b-JGHM-example.pdf` |
| Fig. 2a CLIP risk | `figures/eval-clip-risk.py` -> `figures/data/ghm-data/clip-risk.json` | `figures/Fig2-taskRisks.ipynb` | `figures/output/Fig2-a-CLIP-risk.pdf` |
| Fig. 2b ZSC risk | `figures/eval-zsc-risk.py` -> `figures/data/ghm-data/zsc-risk.json` | `figures/Fig2-taskRisks.ipynb` | `figures/output/Fig2-b-ZSC-risk.pdf` |
| Fig. 2c CDM risk | `figures/eval-cdm-risk.py` -> `figures/data/ghm-data/cdm-risk.json` | `figures/Fig2-taskRisks.ipynb` | `figures/output/Fig2-c-CDM-risk.pdf` |
| Fig. 2d VLM risk | `figures/eval-vlm-risk.py` -> `figures/data/ghm-data/vlm-risk.json` | `figures/Fig2-taskRisks.ipynb` | `figures/output/Fig2-d-VLM-risk.pdf` |
| Fig. 3a-b CLIP and ZSC examples | None; illustration examples, not experimental results | Manual illustration artwork, e.g. Adobe Illustrator/Keynote | `figures/output/Fig3-a-CLIP-loss.pdf`, `figures/output/Fig3-b-ZSC-example.pdf` |
| Fig. 4a-b VLM and CDM examples | None; illustration examples, not experimental results | Manual illustration artwork, e.g. Adobe Illustrator/Keynote | `figures/output/Fig4-a-VLM-example.pdf`, `figures/output/Fig4-b-CDM-example.pdf` |
| Fig. 5 ZSC vs. sample count | `figures/eval-zsc-numsamples.py` -> `figures/data/ghm-data/zsc-numsamples.json` | `figures/Fig56-ZSC-m.ipynb` | `figures/output/Fig5-ZSC-vs-M.pdf` |
| Fig. 6a-c ZSC fitted curves | `figures/eval-zsc-numsamples.py` -> `figures/data/ghm-data/zsc-numsamples.json` | `figures/Fig56-ZSC-m.ipynb` | `figures/output/Fig6-a-ZSC-Fit-Standard TF.pdf`, `Fig6-b-*`, `Fig6-c-*` |
| Fig. 7a-c ImageNet prompt scaling | `scripts/download_data.py` -> `figures/data/imagenet-data/similarities_targets.pt` | `figures/Fig7-imagenet.ipynb` | `figures/output/Fig7-a-imagenet-loss.pdf`, `Fig7-b-*`, `Fig7-c-*` |
| Fig. 8a CLIP OOD risk | `figures/eval-clip-ood.py` -> `figures/data/ghm-data/clip-ood.json` | `figures/Fig8-OODRisks.ipynb` | `figures/output/Fig8-a-CLIP-ood.pdf` |
| Fig. 8b ZSC OOD risk | `figures/eval-zsc-ood.py` -> `figures/data/ghm-data/zsc-ood.json` | `figures/Fig8-OODRisks.ipynb` | `figures/output/Fig8-b-ZSC-ood.pdf` |
| Fig. 8c CDM OOD risk | `figures/eval-cdm-ood.py` -> `figures/data/ghm-data/cdm-ood.json` | `figures/Fig8-OODRisks.ipynb` | `figures/output/Fig8-c-CDM-ood.pdf` |
| Fig. 8d VLM OOD risk | `figures/eval-vlm-ood.py` -> `figures/data/ghm-data/vlm-ood.json` | `figures/Fig8-OODRisks.ipynb` | `figures/output/Fig8-d-VLM-ood.pdf` |
| Fig. 9a-b modality-specific OOD risk | `figures/eval-cdm-ood.py` -> `figures/data/ghm-data/cdm-ood-pt20.json` (fix text `p_t=0.20`, sweep image `p_i`); `figures/eval-vlm-ood.py` -> `figures/data/ghm-data/vlm-ood-pi20.json` (fix image `p_i=0.20`, sweep text `p_t`) | `figures/Fig9-OODrisks2.ipynb` | `figures/output/Fig9-a-CDM-ood.pdf`, `figures/output/Fig9-b-VLM-ood.pdf` |

### Repository Structure

- `scripts/`  
  Shell scripts for running experiments and illustrative examples:
  - `scripts/examples/`: Example scripts for individual tasks (`eg_clip.sh`, `eg_dns.sh`, `eg_sdns.sh`, `eg_nwp.sh`, `eg_snwp.sh`, `eg_clas.sh`)
  - `scripts/experiments/`: Batch experiment scripts for different model configurations (`exp_clip_*.sh`, `exp_cdm_*.sh`, `exp_vlm_*.sh`)
- `saved_models/`  
  Pretrained CLIP models (softmax attention variants available)
- `src/ghmclip/`  
  Main source code package:
  - `src/ghmclip/data/data_random_GHM.py`: Generates random data using various samplers
  - `src/ghmclip/models/model.py`: Defines models for all tasks
  - `src/ghmclip/models/optimizer.py`: Implements optimization algorithms
  - `src/ghmclip/training/`: Training routines for different tasks:
    - `train_CLIP.py`: CLIP contrastive learning
    - `train_CDNS.py`: Conditional denoising (joint training)
    - `train_sequential_DNS.py`: Sequential denoising (fixed CLIP features)
    - `train_NWP.py`: Next-word prediction (joint training)
    - `train_sequential_NWP.py`: Sequential next-word prediction (fixed CLIP features)
    - `train_CLS.py`: Classification tasks
  - `src/ghmclip/utils/`: Utility scripts for configuration and logging
- `figures/` Figure-generation scripts and rendered figures
- `logs/` Log files organized by task type (CDM, VLM) and experiment configurations
- `tests/` Unit tests
- `main.py` Main entry point

## Checkpoints 
All checkpoints are available on Hugging Face: https://huggingface.co/faro1219/multimodal-ghm. Use `python scripts/download_ckpt.py` to download `logs/{CLIP,CDM,VLM}` and stage them as `checkpoints/{CLIP,CDM,VLM}`. Download `logs/CLIP` before running sequential training scripts directly, because sequential CDM/VLM training initializes from CLIP logs.

## Training

To train a model for a specific task, navigate to the `scripts/examples/` directory and run:

```bash
chmod +x ./eg_[task_name].sh
./eg_[task_name].sh
```

Here, `[task_name]` can be one of:

- `clip`: CLIP contrastive learning task
- `dns`: Conditional denoising (jointly trains CLIP and denoising model)
- `sdns`: Sequential denoising (trains only denoising model with fixed CLIP features)
- `nwp`: Next-word prediction (jointly trains CLIP and transformer)
- `snwp`: Sequential next-word prediction (trains only transformer with fixed CLIP features)
- `clas`: Classification tasks

For batch experiments with different configurations, use the scripts in `scripts/experiments/`:

- `exp_clip_*.sh`: CLIP experiments with different model architectures
- `exp_cdm_*.sh`: Conditional denoising experiments
- `exp_vlm_*.sh`: Vision–language model experiments

For more details about each configuration, refer to:

- `src/ghmclip/training/train_[task_name].py`
- `src/ghmclip/utils/config.py`

You can modify the provided shell scripts in `scripts/examples/` and `scripts/experiments/` to change training parameters as needed.

## Citation

If you find this code useful in your research, please cite our paper:

```
@article{oko2025multimodal,
      title={A Statistical Theory of Contrastive Pre-training and Multimodal Generative AI}, 
      author={Kazusato Oko and Licong Lin and Yuhang Cai and Song Mei},
      year={2025},
      archivePrefix={arXiv},
}
```

Thank you for your interest in our work! If you have any questions or encounter any issues, feel free to open an issue or submit a pull request.
